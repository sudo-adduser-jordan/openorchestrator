---
title: Agent adapters
description: The terminal harnesses built into Open Agents and the smaller set that also supports structured Chat.
---

Open Agents compiles agent adapters into the daemon. It uses your locally installed agent CLI and its existing authentication; it does not bundle provider credentials or install adapters from npm.

## Shipped terminal harnesses

The daemon registry currently advertises 26 worker harnesses:

| | | | |
| --- | --- | --- | --- |
| `opencode` | `grok` | | |
| `cursor` | `qwen` | `copilot` | `kimi` |
| `muse` | `droid` | `amp` | `agy` |
| `crush` | `aider` | `goose` | `auggie` |
| `continue` | `devin` | `cline` | `kiro` |
| `kilocode` | `vibe` | `pi` | `kimchi` |
| `prime-agent` | `autohand` | `omp` | |

Run the catalog instead of relying on a static page when scripting:

```bash
open-agents agent ls --refresh
open-agents agent ls --refresh --json
```

The readiness probe reports whether a harness is supported, installed, and authenticated. Spawn still performs authoritative validation.

## Chat versus Terminal UI

All advertised harnesses can run through their native terminal interface. Four also have structured Chat drivers:

| Harness | Chat transport |
| --- | --- |
| `opencode` | Native ACP |
| `droid` | Native ACP |

Every other harness is Terminal UI-only. Choose the initial interface with `open-agents spawn --mode chat|tui`. Compatible OpenCode sessions can hand off between Chat and Terminal UI while retaining the Open Agents session and worktree.

## Choosing and configuring a harness

```bash
open-agents project set-config my-app --worker-agent opencode
open-agents spawn --project my-app --name auth-fix --agent opencode --mode chat
```

`--agent` and `--harness` are aliases. Project configuration may also specify model, permission mode, environment, prompt rules, and separate worker/manager defaults. Use `open-agents project set-config --help` for the typed fields.

<Cards>
	<Card title="OpenCode" description="Terminal UI and native ACP Chat." href="/docs/plugins/agents/opencode" />
	<Card title="Cursor" description="Built-in terminal harness." href="/docs/plugins/agents/cursor" />
	<Card title="Aider" description="Built-in terminal harness." href="/docs/plugins/agents/aider" />
</Cards>
