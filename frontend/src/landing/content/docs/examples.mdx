---
title: Examples
description: Practical workflows for desktop sessions, Chat and TUI, pull requests, reviews, and previews.
---

These examples use capabilities shipped in the current Go daemon and Electron desktop app. They do not rely on the retired TypeScript plugin configuration or an `examples/` directory.

## Start A Focused Chat Worker

1. Add a local GitHub repository in the desktop app.
2. Create a task named **Fix settings validation**.
3. Select an installed Chat-capable harness such as OpenCode or OpenCode.
4. Choose **Chat** and use a bounded prompt:

```text
Reproduce the invalid settings error, add a regression test, and make the smallest fix.
Run the focused tests and summarize changed behavior. Do not refactor unrelated settings code.
```

Watch provider activities and changed files in Chat. If an approval or structured question appears, answer it in the timeline.

## Use A Native Terminal UI

Create another task and choose **Terminal UI**. Open Agents creates the worktree and launches the harness's native interface through tmux on macOS/Linux or conpty on Windows.

Use the desktop terminal for native interactive commands. Send normal follow-up guidance through Open Agents's message controls:

```text
The failing case is on Windows path normalization. Keep the Unix behavior unchanged.
```

## Switch OpenCode Or OpenCode To Chat

For a compatible idle TUI session, choose **Open Chat**. Open Agents stops the old controller, resumes the provider conversation through the native Chat driver, and keeps the same session and worktree.

If the session is busy, choose deliberately:

- **Finish and drain** waits for accepted work to finish before switching.
- **Stop and interrupt** cancels the active turn before switching.

Switching preserves provider context, but a first TUI-to-Chat switch does not reconstruct old terminal screen output as structured Chat cards.

## Claim An Existing Pull Request

The optional CLI can start a worker and associate an existing pull request:

```bash
open-agents spawn --project my-project --agent opencode --name "Finish PR 142" --claim-pr 142
```

Or attach a pull request to an existing session:

```bash
open-agents session claim-pr 142
# From outside the worker, target it explicitly:
open-agents session claim-pr <session-id> 142
```

The shorter form reads the current worker from `OPEN_AGENTS_SESSION_ID`.

Open Agents then observes the GitHub pull request and displays CI, review, and mergeability facts with the session.

## Run A Reviewer

Open a session with a linked pull request, go to **Reviews**, select a configured reviewer harness, and start a review. Reviewer runs are separate from worker harness configuration. Current reviewer harnesses include OpenCode and OpenCode.

Inspect the result, then send actionable findings to the worker. Keep human review in the loop before merging.

## Preview A Web App

Start the development server in the session worktree, then select it explicitly:

```bash
open-agents preview http://127.0.0.1:5173
```

For a repository with `.open-agents/launch.json`:

```bash
open-agents preview start
open-agents preview status
```

Open the Browser tab in the session inspector. Preview targets are session-scoped; Open Agents does not auto-discover one when a worker starts.

## Use Browser Controls From An Agent

When the Browser tab is attached to the session, the optional CLI exposes commands such as:

```bash
open-agents browser snapshot
open-agents browser click <ref>
open-agents browser fill <ref> "hello"
open-agents browser screenshot
```

Temporary network-metadata capture must be started explicitly and expires automatically. It omits request and response bodies plus sensitive values.

## Useful CLI Checks

The CLI talks to the already-running daemon:

```bash
open-agents status
open-agents session ls
open-agents session get <session-id>
open-agents send --session <session-id> --message "Run the targeted test and report the result."
```

The desktop app remains the recommended control surface.

## Next

<Cards>
	<Card title="Quickstart" href="/docs/quickstart" description="Run the first desktop workflow." />
	<Card title="Dashboard" href="/docs/dashboard" description="Understand each desktop surface used above." />
	<Card title="Troubleshooting" href="/docs/troubleshooting" description="Recover when a workflow does not behave as expected." />
</Cards>
